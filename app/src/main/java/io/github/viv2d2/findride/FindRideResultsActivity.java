package io.github.viv2d2.findride;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.widget.TextView;

import org.w3c.dom.Text;

/**
 * Created by SarahZappone on 4/9/17.
 */

public class FindRideResultsActivity extends AppCompatActivity {

    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_find_ride_results);


       // TextView my_ride = (TextView) findViewById(R.id.my_ride);
        /*String from = getIntent().getStringExtra("from");
        String to = getIntent().getStringExtra("to");
        String date = getIntent().getStringExtra("date");
        String time = getIntent().getStringExtra("time");
        String riders = getIntent().getStringExtra("riders");
        String notes = getIntent().getStringExtra("notes");*/
      //  my_ride.setText("Homewood" + "→" + "BWI"); // + "\n" + date + " " + time + " " + riders);

    }
}
